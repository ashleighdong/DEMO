{
  "name": "render-ws-chat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build": "npm --prefix client ci && npm --prefix client run build && rm -rf server/client-dist && cp -r client/dist server/client-dist"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "uuid": "^9.0.1",
    "ws": "^8.16.0"
  }
}
